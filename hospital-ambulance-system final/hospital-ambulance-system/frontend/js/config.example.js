/* ============================================================
   config.example.js — Template for config.js
   ============================================================
   INSTRUCTIONS:
   1. Copy this file to config.js   (in the same folder).
   2. Paste your real Google Maps API key between the quotes.
   3. config.js is gitignored — it will NOT be committed to git.

   Do NOT put a real API key in this file!
   ============================================================ */

// Google Maps Platform API key
// Used by the Google Maps JavaScript API in the browser.
const GOOGLE_MAPS_API_KEY = "YOUR_GOOGLE_MAPS_API_KEY";
